{% extends 'Base.html.twig' %}
{% block contents %}
    <div class="row">
        <div class="col-1">
            <div class="group-content">
                <h1 class="center">Register an Account</h1>
                <p class="lead">Below is a registration form to sign your children up for sporting events throughout Queensland.</p>
                <br>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-1">
            {{ dump(model) }}
            {% block csrf_token_widget %}
                {{ form(form) }}
            {% endblock %}
        </div>
    </div>
{% endblock %}
